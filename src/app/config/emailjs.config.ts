/**
 * WARNING: This static bundle may contain only the EmailJS public key, never the private accessToken.
 * Keep `novarutra.com.br` and `localhost` allow-listed with rate limiting enabled in EmailJS.
 * The template requires {{from_name}}, {{from_email}}, {{company}} and {{message}}.
 */
export const EMAILJS_CONFIG = {
    serviceId: 'service_rw78y0m',
    templateId: 'template_wvx27pj',
    publicKey: 'yBtTVRQzx1ppjdH7w',
} as const;
